# Swift Package Manager build products (258 MB — never commit these)
.build/
.swiftpm/
Package.resolved

# The assembled app bundle and disk image, rebuilt by Packaging/build-app.sh
build/

# macOS
.DS_Store

# Xcode, if it ever gets opened there
*.xcodeproj/
*.xcworkspace/
xcuserdata/

# Signing material must never be committed
*.p12
*.cer
*.provisionprofile

# The seeded design canvas — a ~2.4 MB generated page with the canvas editor
# baked in, rebuilt from Website/Main.dc.html, Website/Phone.dc.html and
# Website/canvas.json. Those are the source; this is the output.
Website/hoot-landing-page.html
